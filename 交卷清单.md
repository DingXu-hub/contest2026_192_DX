# 交卷清单 — contest2026_192_DX（DX 队）

## 0. 一句话状态
PR 已提交（**#1，open，mergeable=true**，源码/文档/日志/Skill/证据全在仓内），
只差：**签 CLA** 与 **拍演示视频 + 硬件照片**。

## 1. 官方四条 + 合并状态

| # | 要求 | 状态 | 位置 |
|---|---|---|---|
| 1 | 技术报告 | ✅ 完成 | `技术报告_contest2026_192_DX.docx` / `.pdf`（9–10 页，按官方模板 3.1–3.7） |
| 2 | 源码 + AI Coding 日志 | ✅ 完成 | `app/hello_app/src/`（应用源码，含 `attitude6.c`、`link.c`、`runtime_skill.c`）；`logs/DingXu-Hub/`（5 会话 / 217 events，`validate-log.py` ALL OK） |
| 3 | 自建 Skill（AI 硬件赛道要求） | ✅ 完成 | `skills/huangshan-watch-dev/SKILL.md`；运行时 Skill：`skills/runtime/{hydrate,stretch,weather}.md` |
| 4 | 真机证据 | ⚠️ **差视频/照片** | `docs/evidence/`（截图 7+1、串口日志 4+2、测量 JSON + 方法说明） |
| ★ | PR 合并进官方队仓 | ⚠️ **差签 CLA** | PR: https://github.com/open-vela/contest2026_192_DX/pull/1 |

## 2. 只差这三步（都需要你本人）

1. **签 CLA**（1 分钟）：用 GitHub 账号 `DingXu-hub`（邮箱 `clonemeoutkm@gmail.com`）到
   https://www.openvela.com/#/community/cla 签署 → 之后在 PR #1 里评论 `/check-cla`
   （机器人上次检查：`cla / cla-check` 已 success，`cla/signature` 为 failure，签完即通过）。
2. **拍演示视频 ≤5 分钟**（必交）：脚本见 `docs/演示视频脚本.md`。建议镜头顺序：
   表盘/六页 UI → `!timer 1` 让"主动提醒"进画面 → 现场敲"天气"演示运行时 Skill→联网取数→上屏 →
   `!att test` 展示 9 用例自证 → 跑步页轨迹（演示模式写入 3417 m 闭环）。
   演示前把 `src/devshot.h` 的 `HUANGSHAN_DEV_SHOT` 保持 0、`APP_DIAG_VERBOSE` 保持 0。
3. **硬件实物照片**：正面点亮表盘、侧面（表带/开孔）、背面、与 PC 串联的整机照各一张。

## 3. 如需重录演示数据（可选）
把 `src/app_diag.h` 的 `APP_DEMO_SEED` 改成 1 → 编译烧录 → 开机即写 5 条历史 + 一条 150 点/3417 m 闭环轨迹
并停在统计页；拍完再改回 0 重烧（发布固件 md5 `406e4c2338be682ae7fb3dffddb058d5`）。

## 4. 关键链接
- PR：https://github.com/open-vela/contest2026_192_DX/pull/1
- Fork 分支：https://github.com/DingXu-hub/contest2026_192_DX/tree/dev-ai-contest-2026
- CLA：https://www.openvela.com/#/community/cla
