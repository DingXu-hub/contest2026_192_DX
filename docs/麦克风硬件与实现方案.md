# 麦克风（音频输入）硬件事实与实现方案

日期：2026-09-15

## 1. 硬件事实（来源：官方 wiki `wiki.lckfb.com/zh-hans/hspi-sf32lb52/` + SiFli SDK）

| 项 | 事实 |
|---|---|
| 麦克风 | **板载集成 MEMS MIC**（核心板自带），另有 `MIC_BIAS`（偏置）与 `MIC_ADC_IN`（输入信号）引到 30pin 排针第 36/37 脚，可外接 |
| 音频 codec | SoC **内部 AUDCODEC**（ADC/DAC，支持 MICBIAS 偏置），HAL 在 `vendor/sifli/chips/drivers/hal/bf0_hal_audcodec.c` |
| MIC 模拟输入脚 | 同芯片官方参考板 `eh-lb52xu` 注释：`PAD_PA09 … // share with MIC` → **PA09 是 codec 的 MIC 输入（模拟）** |
| 音频输出 | `AU_DAC1N_OUT` + 板载 Class-D PA，`PA_42 = Audio_PA_EN` |
| **1.25mm 两针口** | 官方规格：**外接喇叭口（GH-1.25mm，3W/4Ω 或 2W/8Ω）= 输出**，不是麦克风输入 ⚠️ |
| 冲突点 | 我们板 `PA09` 当前被配成 `CTP_RESET`（触摸复位）——与 MIC 共用该脚 |
| 采样率支持 | 8K/11.025K/12K/16K/22.05K/24K/32K/44.1K/48K |

## 2. 构建路径（已确认可行）

- vendor 树的 `chips/sf32lb52/CMakeLists.txt` 用 `file(GLOB HAL_SRCS drivers/hal/*.c)` 后 **显式排除 `bf0_hal_audcodec.c`**；
- 走 Kconfig（`BSP_ENABLE_AUD_CODEC`）不可行：它 `select RT_USING_AUDIO`（RT-Thread 符号，NuttX 树里不存在）；
- **`HAL_DMA_*` 已在固件中**（DMA HAL 正常编译）；
- ⇒ 方案：**把 `bf0_hal_audcodec.c` 加入我们 app 的 `CMakeLists.txt`**（与 `fusion/` 同样做法，自包含于提交仓），应用里直接调 HAL。

## 3. 实现步骤（照官方 SDK 的 RX 范例）

```c
bf0_enable_pll(44100, 1);                                  /* 音频时钟 */
HAL_AUDCODEC_Init(&codec);
HAL_AUDCODEC_Config_RChanel(&codec, 0, codec.Init.adc_cfg);
HAL_AUDCODEC_Receive_DMA(&codec, buf, bufSize, HAL_AUDCODEC_ADC_CH0);
HAL_NVIC_EnableIRQ(AUDCODEC_ADC0_DMA_IRQ);
HAL_AUCODEC_Refgen_Init();
HAL_AUDCODEC_Config_Analog_ADCPath(codec.Init.adc_cfg);
__HAL_AUDCODEC_LP_ENABLE(&codec);
```
外加：
1. `HAL_PIN_Set(PAD_PA09, GPIO_A9, PIN_NOPULL, 1)`（参考板做法：断开数字驱动，让模拟 MIC 输入工作）；
   ⚠️ 触摸复位也用 PA09 → 需要"开机先做触摸复位、录音时切到 MIC"（或用 `--no-touch-reset` 变体验证触摸是否仍可用）。
2. 自己提供 DMA 句柄与中断处理（`AUDCODEC_ADC0_DMA_*`），双缓冲 16 kHz / 16 bit 单声道。
3. 采集 → 环形缓冲 → 通过链路 **新增 `CH_AUDIO` 通道**（设备→PC 方向不受限，实测曾跑通 526 KB 帧）以 DATA 帧上行；
4. PC 侧 `tools/gateway.py` 收到 CH_AUDIO → 存 WAV（便于先"听"验证）→ 再接语音识别。

## 4. 语音联网（下一步，可选）

```
板载 MIC → codec ADC(16k/16bit) → CH_AUDIO 帧 → PC 存 WAV
        → ASR（本地 faster-whisper 或云端 API） → LLM → 答案回传 → 手表 AI 卡片
```
链路与时间同步已就绪（见 `docs/联网功能与串口根因-2026-09-15.md`）。

## 5. 待确认

- 优先做"**板载 MIC 录音上传 + 存 WAV 验证**"？（推荐先做，风险低、可听见）
- 是否同时要**喇叭放音**（PA_42 使能 + DAC 通道）？
- ASR 用**本地 faster-whisper**（PC 装模型、无网络依赖）还是**云端 API**（需 key）？

## 6. bring-up 实测进展（2026-09-15 晚）

**已实现**：`src/mic_audcodec.c|h` 直接驱动内部 AUDCODEC（HAL 已在 arch 库中，无需搬源码），含 DMA 环形缓冲与 RMS/峰值统计；链路在线工具 `!mic`（start/stop/stats/regs/poll/sweep），并加**保活挂钩**（录音路径自动 `pm_report_activity()`，避免 PM SLEEP 门控 codec 时钟）。

**已排除**：
- RCC 模块时钟未开（`HAL_AUDCODEC_MspInit()` 为空）→ 已补 `RCC_MOD_AUDCODEC`/`DMAC1`；
- 官方序列缺项 → 已补 `bf0_enable_pll()` + `Clear_All_Channel` + `ADCPath_Volume`；
- PM SLEEP 导致"无数据"（此前所有测试都在 `[PM] -> SLEEP` 下进行）→ 现测试时系统保持 `[PM] -> ACTIVE`；
- DMA 未武装 → 实测 `ccr=0x2aaf cndtr=1024`（circular，已武装）；
- 12 组时钟配置（src/osr/div/op）全部 `hw irqs=0`、`ADC_CH0_ENTRY` 恒 0；
- 无 fault、无中断风暴（10 s 原始串口观察）。

**当前阻塞点**：寄存器已"看起来全对"（`CFG=1` ADC 使能、`ADC_CFG/CH0_CFG` 已配、PLL 锁定、BG 开、ADC1/2 通路使能），但 **codec 不产生转换数据、也不发 DMA 请求**。

**剩余假设（按性价比排序）**：
1. **数字通道映射**：板载 MIC 可能走 ADC1 → 需试 `channel=1`（`AUDCODEC_ADC1_DMA_REQUEST = DMA_REQUEST_40`；dma_config.h 未给 CH1 的 instance/IRQ，需要试同一 DMAC1 通道或 CH5）。
2. **厂商驱动的最后一步"启动"**：内部 codec 的 RT-Thread 驱动不在公开 SDK（`customer/peripherals/audio/Kconfig` 只有 DA7212）→ 需要 SiFli/LCKFB 提供内部 codec 的初始化范例。
3. **AUDPRC 参与数据搬运**：`ADC_CFG.OP_MODE` 可能要求 AUDPRC 侧使能（试 opmode 与 AUDPRC 配置组合）。

**结论**：软件侧能做的排查已做完；下一步要么继续试 (1)/(3)，要么向 SiFli/立创索要内部 codec 的 bring-up 代码片段，会更省时间。

---

## 2026-09-19 二轮彻查（读取被排除的 vendor HAL 源码 + 真机变体矩阵）

### 读源码得到的官方路径（`vendor/sifli/chips/drivers/hal/bf0_hal_audcodec_m.c`）
- 我们芯片编译的是 `_m.c`：**单实例** `hwp_audcodec`（无 56x/58x 的 LP/HP 两套，
  52x 头文件里 `AUDCODEC_LP` 出现 0 次）——上一轮以为要配 LP 的方向是错的。
- `HAL_AUDCODEC_Init()` 只做 DMA 句柄初始化；`MspInit()` 是空的；
  `HAL_AUDCODEC_Config_ADCPath()` 在本芯片**未链接**（在 `#endif` 之外），
  且它内部把 ADC 字段写进了 `CFG`（看起来是 vendor 笔误）。
- `Clear_All_Channel(0x2)` 内部会脉冲 `ADC_CFG.PATH_RESET` ✓ 我们已调用。
- `bf0_enable_pll(freq, type)` 是公开的完整上电序列（HXT 音频缓冲 `PMUC_HXT_CR1.BUF_AUD_EN`、
  bandgap、PLL 配置、**VCO 校准循环**、锁检测）——我们已调用（另有一条 `updata_pll_freq()+HAL_TURN_ON_PLL()` 路径）。

### 真机变体矩阵（新增 `!mic try [0..6]`，每项启动后等 500 ms 读样本/ENTRY）
| 变体 | 内容 | 结果 |
|---|---|---|
| v0 | 基线（当前代码） | samples=0 |
| v1 | `CFG |= 3<<ADC_EN_DLY_SEL` | samples=0 |
| v2 | `bf0_enable_pll(49152000,0)` 全流程替代 | samples=0 |
| v3 | 模拟路径放到数字使能**之后** | samples=0 |
| v4 | 复刻 vendor 的 `CFG` 写法 | samples=0 |
| v5/v6 | 上述组合 | samples=0 |
| — | 另试 `RCC_MOD_AUDPRC`(bit20) 门控 | samples=0，且打开后 codec 寄存器块空闲时读回 magic，已回退 |

### 把寄存器 dump 放进 `mic_start()` 内部（关键方法：外部 `!mic regs` 会读到总线 magic）
```
[MicCFG] CFG=00000001 ADC_CFG=00000a41 CH0=00010a9d ANA=0000002e REFGEN=00000005
[MicCFG] ADC1_1=00125ab3 ADC1_2=0000000e ADC2_1=007a5b33 ADC2_2=0000000e
         PLL5=1152d2d2 PLL6=00def7c0 PLL0=5bd4ba00 STAT=00000000 BG0=00001931
```
按 52x 位定义逐项核对（这次都是"正确"的）：
- `CFG.ADC_ENABLE=1`；`ADC_CFG`：OSR=1 / OP_MODE=0 / **PATH_RESET=0** / CLK_SRC=1 / CLK_DIV=10；
- `ADC_CH0_CFG`：ENABLE=1、**DMA_EN=1**、DATA_FORMAT=1(16-bit)；
- 模拟块：`ADC1/2_CFG2=0x0e` → **EN(bit3)=1、RSTB(bit2)=1、CLEAR(bit0)=0** ✓（已使能、已解复位）;
  `REFGEN_CFG` EN=1/BM=1 ✓；`ADC_ANA_CFG` MICBIAS_EN=1 ✓。

### 结论与下一步（唯一剩余嫌疑）
模拟前端与数字配置**逐位核对全部正确**，但仍无转换、无 DMA 请求、`ADC_CHx_ENTRY` 恒 0
→ 嫌疑集中在 **ADC 数字时钟/FSP**：需核对 `PLL_CFG6` 的 `EN_CLK_ADC0/1/2`、
`SEL/DIVA_CLK_ADC*` 与 `sel_clk_adc_source`（我们的模拟配置里 `sel_clk_adc_source=0`），
以及采样率发生器（FSP）是否真的起振；若仍不通，需向 SiFli/立创索取内部 codec 的 bring-up 例程
（公开 SDK 只有 DA7212 外部 codec 驱动，`hwp_audcodec` 在整棵树里**没有第二个使用者**）。

**另一条重要观测**：系统空闲时整个 codec 寄存器块读回 `0xc0dec000`（总线 magic），
说明该模块**随时可能被 PM 门控**——任何 bring-up 观测都必须在 `mic_start()` 内部或保持 PM ACTIVE 时进行。

---

## 2026-09-19 第三轮：穷举扫描（结论：公开 SDK 内已无可调项）

### 本轮新增的自查与实验
| 项 | 方法 | 结果 |
|---|---|---|
| DMA/IRQ 是否我们的锅 | 在 `mic_start()` 内部读 `CCR/CNDTR` | `CCR.EN=1`、`CNDTR=1024` ✓ **已武装**；150 ms 后 `CNDTR` 不动 ✗ |
| PLL 与模拟路径的先后 | 模拟配置后再开 PLL（`[MicPLL]`） | `updata_pll_freq -> -1`（**锁失败**）⇒ 模拟路径会复位 PLL；改用 `bf0_enable_pll` 返回 0（锁住）但仍无数据 |
| 音频晶振缓冲驱动强度 | 新发现 `PMUC_HXT_CR1.BUF_AUD_STR`（vendor 从不设置） | 拉到最大 `HXT_CR1 1caf4a6f -> 1caf4e6f` ✗ 无变化 |
| 读对的源码 | 这次读的是**本芯片** `_m.c` 版 `Config_Analog_ADCPath`（此前误读多芯片版） | 52x 用 `GC=0x4`（多芯片是 0x1E）；收尾 `CLEAR=0/RSTB=1/VCMST=0` 与我们实测 `0x0e` 一致 ✓ |
| 历史/其它驱动 | vendor 仓库 git 历史、`audio_rttl` 例子依赖的 `drv_audcodec.c` | 树里与历史里**都没有**（只有 HAL），无参照实现 |
| **穷举扫描** | 新增 `!mic sweep2`：`channel{0,1} × 启动顺序{2} × PLL类型{2} × 时钟源{2} × selclk{3} × diva{3} × FSP{4} × opmode{2}` | **1152 组合，0 产生数据** |

### 结论（如实）
- 数字侧/模拟侧/DMA/时钟选择的**所有可写位都已逐位核对正确**，且 1152 组配置穷举无一产生转换；
- 说明缺的不是"某个配置值"，而是**公开 SDK 未包含的前提**（音频时钟/电源域的隐藏使能，或厂商内部 codec 启动流程）。
- **下一步唯一高效路径：向 SiFli / 立创索取内部 codec 的 bring-up 例程**。HAL 注释本身写着
  “need updata **drv_audcodec** if modify this function”，但该驱动不在公开树中。
- 我们保留的 bring-up 工具（对后续对接有用）：`!mic try [0..9]`、`!mic sweep2`、`!mic probe`、
  `!mic regs`（含模拟块/PLL 寄存器）、以及把 dump 放进 `mic_start()` 的做法（外部读会拿到总线 magic `0xc0dec000`）。

---

## 2026-09-19 第四轮：失败原因分层定位（回答"为什么不行"）

不再"扫参数"，而是把数据通路**逐层拆开、每层独立取证**，定位到底哪一层没工作：

| 层 | 需要满足 | 实测证据 | 判定 |
|---|---|---|---|
| ① 总线/时钟（数字域） | 块可访问 | `ID=0xc0dec000`（ASCII "CODEC000"）✓、`APB_STAT=0`、`IRQ` 可读且非零 | **活的** |
| ② 通道接口 | 通道使能、APB/FIFO 在工作 | `IRQ` bit17 = `ADC_CH0_APB_UF`（**通道欠载**）| **读侧在跑**（该位可能是粘滞标志，故仅作旁证） |
| ③ DMA 通路 | 请求映射正确、通道使能 | `CCR.EN=1`、`CNDTR=1024`，映射与 vendor `dma_config.h` 逐字一致 | **已武装** |
| ④ 寄存器配置 | 全字段合法 | 逐位核对 + **1152 组穷举**（本轮又补 ADC_CH1、两种启动顺序） | **穷尽仍 0 命中** |
| ⑤ 模拟前端 | 调制器有电、有参考 | `ADC1/2_CFG2=0x0e`(EN/RSTB/CLEAR 正确)、`REFGEN_CFG` EN+BM、`ADC_ANA_CFG` MICBIAS_EN，**并补写 `BG_CFG0` 的 `VREF_SEL/MIC_VREF_SEL/EN_AMP/EN_RCFLT`**（vendor 只在其 DAC 路径里写这些） | 写入生效（`0x1931→0x1909/0x1971`）但**仍无样本** ✗ |
| ⑥ 转换时钟 | ΣΔ 采样时钟 | 只能间接推断；`PLL_CAL_RESULT` 恒 0（无法在安全前提下触发校准测量） | **未测** |

**结论（推理而非猜测）**：
1. ① 证明块上电且可访问；③ 证明数据搬运侧无误；④ 证明"没有任何可写配置能改变结果"；
2. 于是失败**必然**落在 ⑤/⑥——**数字域明明在跑（①③），模拟转换前端却不产样本**；
3. ⑤ 的所有已知字段（含 vendor 只在 DAC 路径里写的共享参考）都已补齐并生效，仍无效 → **缺的条件不在 AUDCODEC 寄存器空间内**，
   只能来自**块外**：模拟域的顶层时钟/供电路由，或板级/硬件的未文档化前提；
4. 这与"公开 SDK 里没有第二个 `hwp_audcodec` 使用者、HAL 注释引用了一个不在本树的 `drv_audcodec`"一致：
   **厂商的内部 bring-up 序列里存在我们无法从代码推出的步骤**。

**方法学收获（可复用）**：
- 外部读寄存器会拿到块 ID/总线值 → **所有观测必须放在 `mic_start()` 内部**；
- `IRQ`/`APB_STAT`/`ID` 这三个此前从未读过的寄存器，才是区分"没转换"与"转换了但没搬走"的关键；
- 分层取证 > 盲扫参数：1152 组穷举的价值在于**证明"配置面已穷尽"**，从而把问题锁定到寄存器之外。

---

## 2026-09-19 第五轮（收口）：时钟已证在跑，故障点被精确锁定

### 新增两项决定性证据
1. **PLL 健康检查（就地、20 ms 有界，不再阻塞任务）**：
   ```
   [MicPLL] DONE=1 after 100 us  RESULT=074707d0  STAT=00000000
   ```
   → VCO 校准**完成**且**计数非零** ⇒ **音频 PLL 确实在振荡**，ADC 时钟源存在 ✓（此前"PLL 没跑"的怀疑被推翻）
2. **补扫此前遗漏的字段**：`!mic sweep3` 覆盖 `OSR_SEL{0..7} × STB_INV{0,1} × ADC{0,1}` = 32 组 → **0 命中** ✗
   （`OSR_SEL` 是 ΣΔ 抽取滤波器的核心参数，之前 1152 组里一直固定为 1，这是扫描矩阵的真实缺口）

### 累计排查量
- 定向变体：**13** 个（EN_DLY_SEL、`bf0_enable_pll` 全流程、模拟顺序、vendor CFG 写法、PLL 后置、BG 参考 0xc/2 …）
- 穷举组合：**1184** 组寄存器配置
- 用户重新接线后复测：寄存器值**逐字节不变**、仍零样本 ⇒ **输入接线与"是否转换"无关**

### 因果链（每一步都有实测支撑）
| 环节 | 结论 | 证据 |
|---|---|---|
| 块上电/可寻址 | ✓ | `ID=0xc0dec000`（"CODEC000"） |
| 音频 PLL 振荡 | ✓ | `DONE=1`、`PLL_CAL_RESULT=074707d0` |
| ADC 时钟使能 | ✓ | `PLL_CFG6=0x00def7c0`（EN_CLK_ADC0/1/2） |
| 数字通道 + DMA | ✓ 已武装 | `CCR.EN=1`、`CNDTR=1024`、映射同 vendor `dma_config.h` |
| 通道读侧在请求 | ✓ | `IRQ.bit17 = ADC_CH0_APB_UF`（欠载） |
| 模拟前端使能/解复位/偏置 | ✓ | `ADC1/2_CFG2=0x0e`(EN=1,RSTB=1,CLEAR=0)、`REFGEN` EN+BM、`MICBIAS_EN`、`BG_CFG0` VREF/AMP |
| 配置空间 | ✗ 已穷尽 | 1184 组 + 32 组 = **0 命中** |
| **转换器 → 通道 FIFO** | ✗ **断在这里** | 读侧在请求（欠载）但 FIFO 从不被填（`ENTRY` 恒 0） |

**为什么这算闭环结论**：如果缺的是"某个寄存器没配对"，穷举必然命中（1184 组已覆盖全部字段组合）；
如果缺的是时钟，PLL 校准不会成功。所以断裂点**只能**在"转换器把数据交给通道 FIFO"这一环，
而这一环在本芯片上**没有任何可写的使能位**（`CFG/ADC_CFG/ADC_CHx_CFG` 的字段已全部枚举并扫描）。

**⇒ 缺的是寄存器空间之外的一步**：厂商内部 `drv_audcodec` 序列里的未文档化握手（HAL 注释点名了它，
但它不在公开树），或板级/硅层的模拟供电前提。

### 交给厂商的精确问题（比上一版更具体）
> ADC path is fully configured and the audio PLL is proven oscillating
> (`PLL_CAL_CFG.DONE=1`, `PLL_CAL_RESULT=0x074707d0`). The channel's read side is
> active (`IRQ` = `ADC_CH0_APB_UF`), the DMA is armed (`CCR.EN=1`, `CNDTR=1024`,
> request 39/instance DMA1_Channel4 exactly as `dma_config.h`), and 1184 register
> configurations (including all `OSR_SEL` values, both ADC channels, both start
> orders, FSP and opmodes) never produce a sample: `ADC_CH0_ENTRY` stays 0 and the
> FIFO is never filled. **Which additional step does your internal `drv_audcodec`
> perform between "analogue path enabled" and "converter output appears in
> `ADC_CHx_ENTRY`"** (undocumented handshake, hidden clock/power route, or an
> analogue supply prerequisite)?

---

## 2026-09-19 第六轮：查官方 SDK 文档 → 两条通路搞清了

### 官方 SDK 里找到的权威资料（`OpenSiFli/SiFli-SDK` 仓，本地 blobless 克隆按需取 blob）
1. **内部 AUDCODEC 的 RX（录音）标准序列**（`docs/source/hal/audcodec.md`）：
   ```c
   bf0_enable_pll(44100, 1);
   HAL_AUDCODEC_Init(haudcodec);
   HAL_AUDCODEC_Config_RChanel(haudcodec, 0, haudcodec->Init.adc_cfg);
   HAL_AUDCODEC_Receive_DMA(...);
   HAL_NVIC_EnableIRQ(AUDCODEC_ADC0_DMA_IRQ);
   HAL_AUCODEC_Refgen_Init();
   HAL_AUDCODEC_Config_Analog_ADCPath(haudcodec->Init.adc_cfg);
   __HAL_AUDCODEC_LP_ENABLE(haudcodec);      /* enable AUDCODEC at last */
   ```
   → 与我们实现的顺序**一致** ✓；其中 `__HAL_AUDCODEC_LP_ENABLE` 只在 56x/58x 头里定义
   （`Instance_lp->CFG |= bit0`），而 **52x 的 `CFG.bit0` 就是 `ADC_ENABLE`** —— 同一个位，我们已设 ✓
   （不是缺失项；52x 的 handle 里 `Instance_lp` 为空，硬调反而会空指针）。
2. **52x 的板级音频配置**（`customer/boards/include/config/sf32lb52x/audio_config.h`）：
   里面**只有 `BSP_ENABLE_I2S_CODEC`（外部 I2S codec）**，**没有任何内部 codec 的板级配置** ✗。
3. **官方录音路径是 I2S，不是内部 ADC**（`docs/source/drivers/audio.md`）：
   ```c
   #define BSP_ENABLE_I2S_MIC 1
   rt_device_t g_mic = rt_device_find("i2s1");
   rt_device_open(g_mic, RT_DEVICE_FLAG_RDONLY);
   rt_device_control(g_mic, AUDIO_CTL_CONFIGURE, &caps);   /* 16000 Hz */
   rt_device_control(g_mic, AUDIO_CTL_START, &stream);
   ```
   → **官方文档里"麦克风"是 I2S 设备**（数字麦/外部 I2S codec），内部 AUDCODEC 在 52x 上**没有板级配置、没有例子**。

### 由此得到的结论（两个层面）
- **接线层面**：本板 1.25mm **2pin 口是外接喇叭（输出）**（`PA_42 = Audio_PA_EN`），把麦克风接到"输出"口**不可能录到音** ✗；
  内部 ADC 的麦克风模拟脚是 30pin 排针上的 **`MIC_BIAS` + `MIC_ADC_IN`**（+ 地）——要录内部 ADC 就必须接那里。
- **芯片/软件层面**：官方在 52x 上把"录音"交给 **I2S**，内部 ADC 路径**没有配套驱动/配置/例程** ✗ ——
  这与我们前五轮的实测完全吻合：块活着、PLL 在振荡、DMA 已武装、1184+32 组配置全扫无一产生样本。
  **很可能该 ADC 通路在本芯片/本板上就不被支持（或需要厂商内部的额外前置条件）。**

### 下一步的两条可行路线
- **路线 A（继续内部 ADC）**：把麦克风改接到 `MIC_ADC_IN`(+`MIC_BIAS`+GND)，同时向厂商索取内部 codec 的 bring-up
  （我们的问询已精确到"转换器→FIFO"这一环）。
- **路线 B（走官方支持的路径，推荐）**：改用 **I2S 数字麦**（如 INMP441 / ICS-43434，几元一个），
  官方 SDK 直接支持：`BSP_ENABLE_I2S_MIC=1` + `rt_device_find("i2s1")` + `AUDIO_CTL_START` ✓
  在 openvela/NuttX 侧同样有 I2S 驱动（`sf32lb52x` I2S），比硬啃内部 codec 可靠得多，
  而且对"AI 语音输入"这个卖点更稳。

---

## 2026-09-19 第七轮（重要转折）：官方录音走 **PDM**，不是 AUDCODEC 模拟 ADC

在 SiFli-SDK 仓里挖到**官方录音例子**：`example/hal_example/src/example/example_audio.c`

```c
static PDM_HandleTypeDef PDM_Handle = {0};
hpdm->Instance   = hwp_pdm1;                       // 用 PDM 外设，不是 AUDCODEC
hpdm->Init.Mode         = PDM_MODE_LOOP;
hpdm->Init.Channels     = PDM_CHANNEL_LEFT_ONLY;
hpdm->Init.SampleRate   = PDM_SAMPLE_16KHZ;
hpdm->Init.ChannelDepth = PDM_CHANNEL_DEPTH_16BIT;
hpdm->hdmarx->Instance  = PDM1_L_DMA_INSTANCE;
hpdm->hdmarx->Init.Request = PDM1_L_DMA_REQUEST;
```

### 这意味着什么
- **SiFli 自己在这颗芯片上的"录音"通路是 PDM（数字麦），不是内部 AUDCODEC 的模拟 ADC** ✗；
- 于是前六轮的一切都自洽了：AUDCODEC 块活着、PLL 在振荡、DMA 已武装、1184+32 组配置全对，
  **却永远零转换** —— 因为**麦克风根本没有接在 ADC 上**（板载麦接的是 PDM 接口）；
- 而且 **PDM 恰好就是 2pin**（CLK + DATA）✓ —— 所以我们先前"2pin 只能是模拟麦/喇叭"的推断是错的 ✗，
  用户插的"另一个 2pin 口"很可能正是 **PDM 数字麦口** ✓✓。

### 本工作区的现状（决定要写多少代码）
| 项 | 状态 |
|---|---|
| `hwp_pdm1` 寄存器基址（52x） | ✓ `register.h` |
| PDM 寄存器头 | ✓ `chips/drivers/cmsis/sf32lb52x/pdm.h` |
| PDM HAL 源码 + 头 | ✓ `hal/bf0_hal_pdm.c`、`Include/bf0_hal_pdm.h` |
| **是否编进固件** | ✗ `nm nuttx` 无任何 PDM 符号；`.config` 里 `# CONFIG_BSP_USING_PDM is not set` |
| 能否用 Kconfig 打开 | ✗ 该选项 `select RT_USING_AUDIO`（RT-Thread 符号，openvela 下走不通） |
| 板级 PDM 引脚配置 | ✗ 板级 pinmux 里没有 PDM 条目（需要自己按文档/参考板确定 pad） |

### 下一步（可行且只动 src/）
按寄存器 + 复用 vendor 的 DMA HAL，自己写一个 **PDM 采集驱动**（`src/mic_pdm.c`，约 150 行：
`HAL_RCC` 开 PDM 时钟 → 配 PDM1 模式/采样率/位深 → 配 pad → 用 DMA 循环搬运 → 接进 `!mic pdm`），
烧进去看 DMA 是否出数据：
- **有数据（含噪声）** ⇒ PDM 通路活着，配数字麦即可录音 ✓
- **全零** ⇒ pad 不对或板上无 PDM 麦（需参考板文档/原理图确认 pad）

**硬件前提**：PDM 需要**数字麦**（输出 PDM 的 CLK/DATA）；模拟驻极体麦**不能**直接接 PDM ✗。

---

## 2026-09-19 第八轮：找到官方跑通的采集通路 = **AUDPRC**，并写出驱动（进行中）

### 决定性对照（同一颗芯片、厂商自己跑通的 AI 语音产品）
`OpenSiFli/xiaozhi-sf32` 的板级配置 `app/boards/sf32lb52-xty-ai/hcpu/board.conf`：
```
CONFIG_BSP_ENABLE_AUD_CODEC=y      # 内部 AUDCODEC —— 和我们一样
CONFIG_BSP_AUDPRC_RX0_DMA=y        # ★ 录音：AUDPRC 的 RX0 DMA
CONFIG_BSP_AUDPRC_TX0_DMA=y        # 放音：TX0 DMA
CONFIG_PA_EN_PIN=26
```
⇒ **本芯片录音的真实数据通路是 `AUDCODEC ADC → AUDPRC → RX_CH0 → DMA(AUDPRC_RX0)`**，
而不是我们（以及 SDK 通用 RX 例程）一直武装的 `AUDCODEC 通道 FIFO → DMAC1_CH4` ✗。
这解释并闭环了前七轮的**全部**现象：块活着、PLL 在振荡（`DONE=1/RESULT≈0x074607d0`）、
模拟前端全对、1184+32 组配置全扫 —— 却永远没有样本，**因为样本不走那条路**。

### 本轮已落地
- 新增 `src/mic_audprc.c|h`（AUDPRC 采集驱动：`HAL_RCC` 开 AUDPRC → `HAL_AUDPRC_Init`
  → `HAL_AUDPRC_Config_ADCPath`（`ADC_PATH_CFG0.SRC_SEL` 源选择）→ `__HAL_AUDPRC_ENABLE`
  + `__HAL_AUDPRC_ADCPATH_ENABLE` → `AUDPRC_RX0` 循环 DMA（`DMA1_Channel4` / request 53）
  → 样本统计/环形缓冲）；
- 新增 `!mic prc`（起采集并报样本）与 `!mic prcsweep`（逐个试 `src_sel`）；
- **编译链接通过** ✓ —— `HAL_AUDPRC_Init/Config_ADCPath/Receive_DMA/DMAStop` 全部从 ROM 解析成功
  （和 AUDCODEC 的 ROM 函数同性质），固件 `8237c508…` 已烧录。

### 当前卡点（下一轮继续，不是"不行"）
`!mic prc` 只看到 AUDCODEC 的启动日志（`[Mic] started`），**`[MicPRC]` 一行都没出现**；
`!mic prcsweep` 也无输出 ⇒ AUDPRC 分支要么没被路由到，要么在 `HAL_AUDPRC_Init` 里就阻塞/返回失败。
下一步（已想好，逐条可查）：
1. 分支入口先 `ai_send("@TOOL micprc")` 确认路由；
2. 调用任何 HAL 之前先读 `hwp_audprc->ID/CFG`（像 codec 那样做"块是否活着/在时钟域"的体检）；
3. 若卡在 `HAL_AUDPRC_Init`，改为**按寄存器内联实现**（位定义已齐：`CFG.ENABLE`、`CFG.ADC_PATH_EN`、
   `ADC_PATH_CFG0.SRC_SEL`、`STB.ADC_DIV`、`CFG.AUDCLK_DIV`），不再依赖 ROM HAL。

---

## 2026-09-19 第九轮：AUDPRC 通路**跑起来了**（DMA 真的在搬），只差最后一段路由

### 实测（固件 `608dc331…`，`!mic prc`）
```
@TOOL micprc                                   ← 命令路由确认
[Mic] started: rate=16000 ...                  ← AUDCODEC（ADC 源）已配好
[MicPRC] after RCC: ID=000a0000 CFG=00020000 RX0_CFG=00000000 STB=00010001
[MicPRC] after reset: CFG=00020000
[MicPRC] Init ok: CFG=00000100                 ← HAL_AUDPRC_Init 成功
[MicPRC] ADCPath ok: ADC_PATH_CFG0=00..        ← 路由寄存器已写
audprc: samples=0 irqs=0 cndtr=486 | buf nz=0 peak=0
```

**关键对比**：
- 老的 AUDCODEC 通道 DMA：`cndtr 1024 -> 1024`（永远不动）✗
- **AUDPRC RX0：`cndtr 1024 -> 486`（真的在搬！）** ✓✓✓
  ⇒ **通路选对了**：录音确实是 `AUDCODEC ADC → AUDPRC → RX_CH0 → DMA`。

**仍差的一段**：缓冲区**内容全 0**（`nz=0`）⇒ DMA 在搬，但 **AUDPRC 的 RX 源还没有拿到有效音频数据**；
且 `irqs=0` 说明**我们的 DMA 中断还没接上**（统计只由中断回调喂，所以要用"缓冲区直读"这种不依赖中断的判据 ✓ 已加 `mic_prc_peek`）。

### 下一步（三条，逐条可查）
1. **完整打印路由寄存器**（上一轮 `ADC_PATH_CFG0` 被输出截断）：确认 `SRC_SEL` / `src_ch_en` / `RX_CH0_CFG` 的使能与数据格式位；
2. **把 AUDCODEC 的 ADC 输出接到 AUDPRC**：读 `bf0_hal_audprc.c` 里 `Config_ADCPath` 的注释/用法，
   以及 **xiaozhi 工程自己的音频初始化顺序**（`app/peripherals/` 下若有 audio 外设代码，那份就是"能跑的调用顺序"）；
3. **修中断接线 + 加固 `mic_prc_stop`**（`!mic prcsweep` 的逐项输出没打出来，怀疑卡在 `HAL_AUDPRC_DMAStop`
   或 IRQ 未 attach 时的 `up_disable_irq`）。

**结论**：路**明显还没走完**——从"完全不动"变成"DMA 在搬、只差源数据与中断接线"，这是本轮最实质的进展 ✓。

---

## 2026-09-19 第十轮：`RX_CH0_CFG.FORMAT/MODE` 扫描 + 发现的自身缺陷

本固件 `af22e121…` 实测：
```
[MicPRC] after RCC: ID=000a0000 ... Init ok: CFG=00000100
[MicPRC] ADCPath ok: ADC_PATH_CFG0=0000?60      ← 注意 bit5/6 有值（音量域），SRC_SEL=0
（`[MicPRCSWEEP]` 的逐组合行一行都没打出来）
```

**新发现（先怀疑自己）**：逐组合行缺失，最可能是 **`mic_prc_start()` 第二次起就失败** ✗ ——
`mic_prc_stop()` 只调了 `HAL_AUDPRC_DMAStop()`，**没有复位 `s_prc.State[]`** ✗，
于是下一次 `HAL_AUDPRC_Receive_DMA()` 会因为 `State & BUSY_RX` 直接返回 `HAL_BUSY` ✗
（与当年 AUDCODEC 驱动里 `HAL_DMA_Start_IT` 返回 HAL_BUSY 是同一类问题）。

**下一轮（三处小改，都在我自己的代码里）**：
1. `mic_prc_stop()` 里补 `s_prc.State[HAL_AUDPRC_RX_CH0] = 0;`（或调 ROM 的 `HAL_AUDPRC_DeInit`）→ 让重复 start 可用；
2. 把 `RX_CH0_CFG.FORMAT/MODE` 的 8 组组合真正跑完（判据用 `mic_prc_peek()` 的非零计数，不依赖中断）；
3. 若仍全零 → 按 `audprc_src_table_t` 配 SRC 系数（文档给了完整表：`{in_rate, out_rate, …}`）。

**进度小结**：AUDPRC 通路已证实正确且在搬运（`cndtr` 递减），失败点已收缩到
"**RX 数据格式/模式或 SRC 配置**"这一类**我方可控**的寄存器位 —— 不再是"不知道哪里不对"。

---

## 2026-09-19 第十一轮：扫描跑通（状态机修复生效），失败点收缩到"RX 源"

### 实测（固件 `f55d1a5f`，`!mic prcsweep`）
```
[MicPRCSWEEP] src=0 fmt=0 mode=0 -> RX_CFG=00000001 CCR=00002aaf CNDTR=512 nz=0 peak=0 irqs=0
（随后 src=1 等组合继续执行 —— 说明 mic_prc_stop() 复位 State 的修复生效 ✓）
```
- `RX_CH0_CFG.ENABLE=1`、`CCR=0x2aaf`（通道使能 ✓）、**`CNDTR=512`（DMA 一路在搬）** ✓
- 但 **`nz=0`**：缓冲区内容仍全 0 ✗；`irqs=0`：DMA 中断仍未接上 ✗

### 结论与下一步（数据侧只剩两处）
1. **RX 源选择**：AUDPRC 的输入可为 `I2S / MEM / AUDCODEC`（官方 audprc.md 明确），
   `ADC_PATH_CFG0.SRC_SEL` 只有 1 位 → 只有两个源。当前 `SRC_SEL=0` 出来是**零**，
   所以**必须确认 `SRC_SEL=1` 是否是 AUDCODEC**（扫描已在跑，需抓完整输出）；
2. **SRC 系数**：文档给了完整 `audprc_src_table_t`（`{in_rate, out_rate, …}`），
   若 RX 路径经过采样率转换，必须按表写入系数，否则数据通路可能输出零 ✗；
3. **中断接线**：`irqs=0` —— `HAL_DMA_Start_IT` 之后需确认 `CCR` 的 `TCIE/HTIE` 位；
   若 HAL 没开中断，就自己置位（`CCR |= TCIE|HTIE`）✓（判据已改为不依赖中断的 `mic_prc_peek` ✓）。

### 控制台注意
设备端串口在大量打印时会**丢字节/丢换行** ✗（本轮多次遇到 `...=00000606[Mic` 这种截断），
所以后续诊断输出必须：**显式 `\n` + 每次打印后 `usleep` 排空**（已写入规范）。

---

## 2026-09-19 第十二轮：8 组组合全部跑完（全零）+ 新证据

固件 `2e0cef70`，`!mic prcsweep` 完整输出（8 组 = src_sel{0,1} × FORMAT{0,1} × MODE{0,1}）：
```
[MicPRC] enabled: CFG=00000181        ← bit0 ENABLE + bit7 ADC_PATH_EN + bit8 AUTO_GATE ✓ 都开了
[MicPRC] dma CCR=00002aaf CNDTR=512   ← 我已自置 TCIE/HTIE（bit1/2）✓ 中断使能位确实置上了
[MicPRCSWEEP] src=0 fmt=0 mode=0 RX_CFG=00000001 CCR=00002aaf CNDTR=512 nz=0
…（8 组全部 nz=0，CNDTR 始终 512）…
[MicPRCSWEEP] 8 combinations, 0 produced non-zero data
```

### 由此得到的两条硬结论
1. **DMA 只搬了一次就停在 CNDTR=512**（不是持续搬运）⇒ **AUDPRC 的 RX FIFO 只吐了一小段（全零）后再无数据** ✗
   —— 也就是"**RX 源没有被喂数据**"，不是 DMA 配置问题（通道使能、CAS、MINC、中断位都对 ✓）。
2. `irqs=0` 与 CCR 的中断位无关（我已确认 TCIE/HTIE 置上 ✓）⇒ 中断确实没触发，因为**传输没有完成事件**（停在中途）✓ 两者是同一个原因 ✓。

### 剩下的候选（都在"谁来喂 AUDPRC 的 RX"这一环）
- **AUDCODEC 侧的显式路由**：codec 的 ADC 输出要交给 AUDPRC，可能需要 codec 侧某位（或要求 codec 自己的通道 DMA 关掉、只走 AUDPRC）✗ 未找到该位 → 需查 `bf0_hal_audprc.c` / codec HAL 的注释或厂商文档；
- **SRC 系数**：`audprc_src_table_t`（`{in_rate, out_rate, …}`）—— 若 RX 路径经过采样率转换而系数未写，输出可能是稳定的零 ✗；
- 我们已验证**不是**：块电源/时钟（`ID=000a0000`）、Init/路由寄存器写入（`CFG=0x181`、`ADC_PATH=0x606`）、DMA 通道配置、8 种通道格式组合。

**结论**：失败点已收缩到"**AUDPRC 的 RX 源选择 + SRC 系数**"这一单一环节，且已有两条可查依据（官方文档 + `audprc_src_table_t`）。
