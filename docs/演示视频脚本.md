# 演示视频脚本（目标 ≤ 5:00）

> 原则：**只演示真实跑通的功能**；限制如实说明；重点突出 AI 赛道的"主动 + 执行"。
> 建议 1080p / 横屏；屏幕特写用手机靠近拍（避免反光），终端部分用录屏（Windows：OBS 或 Win+G）。

---

## 0:00–0:25 开场（25 s）

| 画面 | 口播 / 字幕 |
|---|---|
| 手表戴在手上，表盘特写（时间/日期/步数环） | "这是我们的参赛作品——**黄山派智能跑步手表**，队伍 **DX队**（contest2026_192_DX）。" |
| 板子+屏幕的实物全景 | 字幕：LCKFB 黄山派 Pi（SF32LB52）· openvela/NuttX · 390×450 AMOLED |
| 一句话定位 | "它在**设备上**跑了一个能**主动**行动、能**执行**操作的 AI Agent。" |

## 0:25–1:15 硬件与系统（50 s）

| 画面 | 口播 |
|---|---|
| 按键翻页，快速过 6 页：表盘→跑步→轨迹→指南针→统计→设置 | "整套手表固件从零自研：表盘、跑步、轨迹、指南针、统计、设置六页；只用两个物理按键翻页，触摸只做页面内操作，全程无长按、四角不留控件。" |
| 传感器特写（板载 IMU/磁力计区域）+ 串口日志一行行滚动 | "传感器是板载 LSM6DS3 九轴 IMU 与 MMC5603NJ 磁力计，驱动与融合算法都是自己写的。" |
| 跑一下终端编译输出（可选，1-2 s） | 字幕：CMake/Ninja + arm-none-eabi · 编译 → 烧录 → 串口验证 |

## 1:15–2:00 跑步核心（45 s）

| 画面 | 口播 |
|---|---|
| 跑步页：单击 `START`，计时/距离开始走；走动几步 | "单击开始，手表用 IMU 做步数、配速与**航位推算**——因为没有 GNSS，轨迹完全靠惯性推算。" |
| 切到轨迹页：轨迹实时生长、拖动平移、双指缩放、长按切海拔 | "轨迹实时生长，可拖动缩放；这是自研的航迹渲染。" |
| 回到跑步页，长按结束 → 完成卡片 | "长按结束并保存，数据进入统计页。" |

## 2:00–2:35 指南针（35 s）

| 画面 | 口播 |
|---|---|
| 指南针页：转动板子，表盘反向旋转、中央度数变化 | "指南针用 xioTechnologies/Fusion 九轴融合（NED），磁力计做了硬磁/软磁校准、轴重映射与"每次测量自动 SET/RESET 去磁"。" |
| 点 `CALIBRATE`，出现倒计时环 + X/Y/Z 覆盖条，画 8 字/翻滚 | "双击进入 15 秒校准，屏幕上给出 X/Y/Z 三轴实时覆盖提示；覆盖不足会被拒绝，避免把错误数据写进校准。" |
| 特写 | 字幕（**如实**）：靠近金属/大电流时场强异常会自动降级为纯陀螺（界面显示 GYRO），这是磁力计的物理边界。 |

## 2:35–4:05 AI Agent（90 s，重点）

| 画面 | 口播 |
|---|---|
| 终端（Windows）输入 `py -3 tools/llm_relay.py auto ***` | "AI Agent 跑在手表里，交互渠道是串口 CLI；PC 端脚本只负责把 LLM 请求转发到云端（密钥只在命令行传入、不落盘）。" |
| 输入 `!status` → 手表屏幕弹出状态卡片 | "**执行能力**：`!status` 直接调设备状态——运行状态、里程、指南针、电源估计。" |
| 输入 `!start` → 跑步页开始计时 | "`!start` 真的启动一次跑步。" |
| 输入 `?帮我看看现在的状态，给一句建议` → 数秒后屏幕弹出中文答案 | "**LLM 通路**：问题带着设备状态发给 DeepSeek，答案回写到手表屏幕上。" |
| 输入 `!timer 1`，快进到 1 分钟后，手表**无人操作**自动弹出提醒 | "**主动能力**：这是重点——它不等你问。设置提醒后到点自己弹；空闲 3 分钟还会主动请 LLM 生成一句教练提示。" |
| 字幕 | "Agent / 工具 / 主动引擎全部运行在设备上；LLM 网络出口经 PC 中继（板子无 WiFi，SoC USB 未引出，如实说明）。" |

## 4:05–4:35 AI 协作与工程沉淀（30 s）

| 画面 | 口播 |
|---|---|
| 展示 `skills/huangshan-watch-dev/SKILL.md` 与 `logs/` 结构 | "开发全程使用 AI Coding，并把反复使用的流程沉淀成 Skill（同步→编译→烧录→串口验证→截图复核），AI 对话日志按大赛要求归集提交。" |
| 展示交接文档目录/关键结论一行 | "连串口"收方向只在 /dev/console"、"SLIP 发送需补 carrier 位"这类结论都写进了文档，方便复现。" |

## 4:35–5:00 收尾（25 s）

| 画面 | 口播 |
|---|---|
| 六页快速轮播 + 结尾定格在表盘 | "限制如实说明：无 GNSS（轨迹为推算精度）、无 RTC（演示时钟）、LLM 需 PC 中继。后续计划接入音频与真实联网，把手表做成更完整的腕上助手。" |
| 字幕 | 队伍/仓库：contest2026_192_DX · 感谢观看 |

---

## 拍摄清单（开拍前逐项打勾）

- [ ] 手表电量/供电稳定；屏幕无划痕反光（侧光/柔光）
- [ ] 终端窗口字号调大（16–18 pt），只显示关键几行
- [ ] 提前验证：`!status` / `!start` / `?问题` / `!timer 1` 各跑通一次（本脚本依赖它们）
- [ ] 主动提醒镜头需要真实等待 1 分钟：可先设 `!timer 1` 再拍其它镜头，回来正好响
- [ ] 录屏与实拍分开录，后期拼接；终端命令可在后期加"键入"字幕
- [ ] 全程配音口播或纯字幕（二选一，别混）
- [ ] 成片压到 5 分钟以内（480p 以上即可，mp4）
