# 演示视频脚本 v2（按当前真机固件重写，目标 ≤ 5:00）

> 原则：**只演真实跑通的功能**，限制如实说明；重点突出 AI 赛道的"**主动 + 执行**"。
> 当前设备状态（开拍前已就绪）：固件含**演示跑步**（自动开跑、轨迹画圈，**模拟数据**）、网关已联网并同步真实时间。
> 需要真实 LLM 回复的镜头：用你自己的 key 重启网关 → `py -3 tools\gateway.py COM4 --api-key sk-xxxx`

---

## 0:00–0:20 开场
| 画面 | 口播 / 字幕 |
|---|---|
| 表盘特写（真实时间、步数环） | "参赛作品 **黄山派智能跑步手表**，队伍 **DX队**（contest2026_192_DX）。" |
| 板子+屏幕实物 | 字幕：LCKFB 黄山派 Pi（SF32LB52）· openvela/NuttX · 390×450 AMOLED |
| 定位 | "它在**设备上**跑着一个能**主动**行动、能**执行**操作的 AI Agent。" |

## 0:20–1:00 六页 UI（40 s）
| 画面 | 口播 |
|---|---|
| 按 **KEY1/KEY2** 依次翻：表盘→跑步→轨迹→指南针→统计→设置 | "六页固件从零自研；**只用两个按键翻页**，触摸只用于页面**中轴大按钮**，**全程无长按、四角不留控件**。" |
| 轨迹页停留 2 s（轨迹在生长、画圈） | "轨迹实时生长、自动适配视野。"（**口播必说：这是模拟跑步数据，方便演示航位推算**） |
| 设置页亮一下 | "设置里可切换单位、亮度、**归零航向**。" |

## 1:00–1:40 跑步与姿态（40 s）
| 画面 | 口播 |
|---|---|
| 跑步页：中轴按钮单击开始 → 计时/距离/配速在走 | "单击开始；步数、配速、距离都用 IMU 算，**无 GNSS**，轨迹是**航位推算**。" |
| 指南针页：面板显示 `6-AXIS REL`、`ROLL/PITCH/RATE` | "姿态是**6 轴（陀螺+加速度）**、基于官方 Fusion 库的无磁路径；航向是**相对方位**——本机磁力计硬铁误差达 27 µT，无法可信验证，所以磁力计**不参与航向**，界面明确标注 REL 并提供 `SET 0` 归零。" |
| 中轴 `SET 0` 点一下 | "一键把当前朝向设为零，之后转弯按相对角度显示。" |

## 1:40–2:30 自证式验证（50 s，亮点）
| 画面 | 口播 |
|---|---|
| 终端输入 `!att test`，屏幕/串口逐条打 `[PASS]` | "**最关键的工程做法**：姿态模块内置**合成真值自检**——用已知姿态与角速度生成输入，比对官方链路输出，9 个用例全过（平放/左右倾/抬头/复合姿态/±60dps 偏航积分/倾斜转弯/毛刺抑制）。" |
| 字幕 | "它固定了符号、单位与坐标系；开发中正是它抓出了两处符号错误和一次"真实转身被丢弃"的缺陷。" |
| 终端输入 `!att` | "实时读数：`yaw/roll(acc)/pitch(acc)/rate/dt`，加速度参考与滤波值同屏对照。" |

## 2:30–3:40 AI Agent：执行 + 主动（70 s，重点）
| 画面 | 口播 |
|---|---|
| 终端输入 `!status` → 手表弹状态卡片 | "**执行能力**：`!status` 直接调设备状态（跑步/里程/航向/电源）。" |
| 输入 `!start`（若在跑可 `!stop`） | "`!start` 真的启动一次跑步。" |
| 输入 `!coach` → 弹出教练卡片 | "**AI 教练**：把**实时跑步数据**（距离/用时/配速/步频/步数）组织成 prompt，经 PC 网关问 LLM，回复以卡片上屏。" |
| 口播 + 字幕 | "更有意思的是**主动性**：跑步进行中**每 3 分钟设备自己触发**一次教练——**设备决定何时，LLM 决定说什么**。" |
| 输入 `!timer 1`，随后等 1 分钟（可先设好再拍别的镜头） | "`!timer` 到点**无人操作也会自己弹提醒**。" |

## 3:40–4:20 运行时 Skill + 联网（40 s）
| 画面 | 口播 |
|---|---|
| 输入 `!skills` → 列出 3 条技能 | "**运行时 Skill**：启动时从 `/data/agent/skills/` 播种并解析 markdown 技能（name/trigger/action），**改动无需重刷固件**。" |
| 直接敲 "**天气**"（不加任何命令符号） | "自由文本命中 trigger → 自动执行 `http` 动作 → 经网关取回**真实天气** → 卡片上屏。" |
| 终端滚一行 `HTTP 200` | 字幕：帧协议（CRC16+ACK/重传）+ PC 网关；时间同步后表盘显示**真实本地时间**。 |

## 4:20–5:00 工程沉淀 + 收尾（40 s）
| 画面 | 口播 |
|---|---|
| `skills/huangshan-watch-dev/SKILL.md` + `logs/` | "开发全程用 AI Coding，把流程沉淀成 Skill（同步→编译→烧录→串口验证→抓帧复核），AI 对话日志按大赛要求归集。" |
| 技术报告/证据目录闪一下 | "性能与结论都有测量方式与日志：如 SRAM 由 81.8% 降到 38.2%、姿态静止 3 分钟漂移 +0.03~0.10 °/min、`!att test` 9/9 PASS。" |
| 回到表盘定格 | "**如实说明**：无 GNSS（轨迹为推算）、无 RTC（依赖网关对时）、**麦克风已接板载 MEMS 麦并做了完整排查但未跑通**（16 轮分层取证、1288 组寄存器穷举、官方资料逐条对照，文档里全部可复现）；LLM 出口经 PC 中继。" |

---

## 开拍前清单（逐项打勾）
- [ ] 网关在跑（真实时间已同步）✓ 若拍 LLM 回复镜头，先用 `--api-key` 重启网关
- [ ] 终端字号 16–18 pt，只留关键几行
- [ ] 预演：`!status`、`!att test`、`!coach`、`!skills`、"天气"、`!timer 1` 各跑通一次
- [ ] "主动提醒"镜头要真实等 1 分钟：**先设 `!timer 1` 再拍别的镜头**，回来正好响
- [ ] 手机拍屏幕注意反光（侧光）；终端用 OBS/Win+G 录屏，后期拼接
- [ ] 统一中文口播（或纯字幕，二选一）
- [ ] 成片 ≤5 分钟、mp4、≥480p；**模拟跑步处务必口播说明**
